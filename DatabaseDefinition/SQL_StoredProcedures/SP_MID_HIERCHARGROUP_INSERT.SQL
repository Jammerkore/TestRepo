CREATE PROCEDURE [dbo].[SP_MID_HIERCHARGROUP_INSERT] 
	@HCG_ID VARCHAR(50),
	@HCG_RID INT OUTPUT
AS
BEGIN
	SET NOCOUNT ON

	INSERT INTO HIER_CHAR_GROUP (HCG_ID)
	VALUES (@HCG_ID)

	SELECT @HCG_RID = SCOPE_IDENTITY() -- TT#3256 - JSmith - Replace @@IDENTITY with SCOPE_IDENTITY()
END
GO


