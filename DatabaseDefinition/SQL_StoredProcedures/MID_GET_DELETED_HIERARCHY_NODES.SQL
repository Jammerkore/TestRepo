CREATE PROCEDURE [dbo].[MID_GET_DELETED_HIERARCHY_NODES] 
AS
BEGIN
	SELECT 
	    HN_RID, 
		HOME_PH_RID, 
		HOME_LEVEL 
	  FROM HIERARCHY_NODE WITH (NOLOCK)
	  WHERE NODE_DELETE_IND = '1'
	  ORDER BY HOME_PH_RID, HOME_LEVEL DESC
	
END
GO